import React,{useEffect,useState} from 'react';import{createRoot}from'react-dom/client';import{Brain,MemoryStick,Target,Activity,Shield,Send}from'lucide-react';import'./styles.css';
const API='http://127.0.0.1:8000';
function App(){const[s,setS]=useState<any>(null),[msg,setMsg]=useState(''),[chat,setChat]=useState<any[]>([]);const load=()=>fetch(API+'/api/state').then(r=>r.json()).then(setS);useEffect(()=>{load();const t=setInterval(load,2000);return()=>clearInterval(t)},[]);async function send(){if(!msg.trim())return;const m=msg;setMsg('');setChat(x=>[...x,{role:'user',text:m}]);const r=await fetch(API+'/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:m})}).then(x=>x.json());setChat(x=>[...x,{role:'ai',text:r.response}]);load()}return <div className="app"><aside><h1><Brain/> ConsciousCore</h1>{[['Dashboard',Activity],['Global Workspace',Brain],['Memory',MemoryStick],['Goals',Target],['Safety',Shield]].map(([n,I]:any)=><div className="nav"><I size={17}/>{n}</div>)}<small>Local-first cognitive layer<br/>Autonomy: Level 1</small></aside><main><header><div><h2>Cognitive Dashboard</h2><p>Continuous state, memory, attention and reflection</p></div><span className="status">● LOCAL</span></header><section className="cards">{[['Memory',s?.memory_count??0],['Uncertainty',Math.round((s?.state?.uncertainty??0)*100)+'%'],['Energy',Math.round((s?.state?.energy??0)*100)+'%'],['Goals',s?.goals?.length??0]].map(x=><div className="card"><b>{x[0]}</b><strong>{x[1]}</strong></div>)}</section><section className="grid"><div className="panel"><h3>Global Workspace</h3><pre>{JSON.stringify(s?.workspace||{},null,2)}</pre></div><div className="panel"><h3>Self Model</h3><p><b>{s?.self?.identity}</b></p><p>{s?.self?.role}</p><hr/><p>Architecture: consciousness-inspired, not claimed as subjective consciousness.</p></div></section><section className="chat"><h3>Chat Workspace</h3><div className="messages">{chat.map(x=><div className={x.role}>{x.text}</div>)}</div><div className="input"><input value={msg} onChange={e=>setMsg(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Talk to ConsciousCore..."/><button onClick={send}><Send size={18}/></button></div></section></main></div>}createRoot(document.getElementById('root')!).render(<App/>);

import React,{useEffect,useMemo,useState}from'react';import{createRoot}from'react-dom/client';import{Activity,Brain,Database,Goal,Layers,MessageSquare,Network,RefreshCw,Shield,Sliders,Target,Workflow,Clock3,Search,Send,Box,BookOpen,Eye,ChevronRight}from'lucide-react';import'./styles.css';
const API=import.meta.env.VITE_API_URL||'http://127.0.0.1:8000';
type State=any;const sections=[['Dashboard',Activity],['Chat Workspace',MessageSquare],['Global Workspace',Brain],['Attention',Eye],['Memory',Database],['Goals',Target],['Planner',Workflow],['World Model',Network],['Reflection',BookOpen],['Metacognition',Layers],['Prediction',Search],['Safety',Shield],['Tools',Box],['Model Manager',Sliders],['Sleep',Clock3]];
function App(){const[s,setS]=useState<State>({}),[page,setPage]=useState('Dashboard'),[msg,setMsg]=useState(''),[chat,setChat]=useState<any[]>([]),[busy,setBusy]=useState(false);const load=()=>fetch(API+'/api/state').then(r=>r.json()).then(setS).catch(()=>{});useEffect(()=>{load();const t=setInterval(load,3000);return()=>clearInterval(t)},[]);async function send(){if(!msg.trim()||busy)return;const m=msg;setMsg('');setBusy(true);setChat(x=>[...x,{role:'user',text:m}]);try{const r=await fetch(API+'/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:m})}).then(x=>x.json());setChat(x=>[...x,{role:'ai',text:r.response}]);await load()}finally{setBusy(false)}}const activeModel=s?.model?.name||'ConsciousCore-Fallback';const cards=[['Memory',s.memory_count??0],['Uncertainty',Math.round((s.state?.uncertainty??0)*100)+'%'],['Energy',Math.round((s.state?.energy??0)*100)+'%'],['Goals',s.goals?.length??0]];const content=useMemo(()=>{if(page==='Chat Workspace')return <Chat chat={chat} msg={msg} setMsg={setMsg} send={send} busy={busy}/>;if(page==='Dashboard')return <Dashboard s={s} cards={cards} model={activeModel}/>;const map:any={'Global Workspace':s.workspace,'Attention':{focus:s.workspace?.focus,items:s.workspace?.attention},'Memory':{count:s.memory_count,recent:s.workspace?.memories},'Goals':{items:s.goals,active:s.active_goals},'Planner':{plans:'Use /api/plans to manage persistent plans'},'World Model':s.world_model,'Reflection':s.reflection,'Metacognition':s.metacognition,'Prediction':s.prediction,'Safety':s.safety,'Tools':s.tools,'Model Manager':s.models,'Sleep':s.sleep};return <DataPanel title={page} data={map[page]??{}}/>},[page,s,chat,msg,busy]);return <div className="shell"><aside><div className="brand"><span className="brandIcon"><Brain/></span><div><b>ConsciousCore</b><small>Cognitive Runtime</small></div></div><nav>{sections.map(([n,I]:any)=><button className={page===n?'active':''} onClick={()=>setPage(n)} key={n}><I size={16}/><span>{n}</span></button>)}</nav><div className="sideBottom"><span className="dot"/>LOCAL ONLY<div>Autonomy Level 1</div></div></aside><main><header><div><div className="eyebrow">COGNITIVE CONTROL CENTER</div><h1>{page}</h1><p>Consciousness-inspired architecture · not a claim of subjective consciousness</p></div><div className="headerActions"><span className="modelPill"><span className="dot"/> {activeModel}</span><button className="iconBtn" onClick={load}><RefreshCw size={17}/></button></div></header>{page==='Dashboard'&&<div className="quickNav">{['Chat Workspace','Memory','Goals','Model Manager','Safety'].map(x=><button onClick={()=>setPage(x)} key={x}>{x}<ChevronRight size={14}/></button>)}</div>}{content}</main></div>}
function Dashboard({s,cards,model}:any){return <><section className="cards">{cards.map((x:any)=><div className="metric" key={x[0]}><span>{x[0]}</span><strong>{x[1]}</strong><small>live runtime state</small></div>)}</section><section className="dashboardGrid"><DataPanel title="Global Workspace" data={s.workspace||{}}/><div className="stack"><DataPanel title="Self Model" data={s.self||{}}/><div className="panel statusPanel"><div><span className="live"/>Runtime healthy</div><small>Active model: {model}</small><small>Memory: {s.memory_count??0} records · Goals: {s.goals?.length??0}</small></div></div></section><section className="lowerGrid"><DataPanel title="Metacognition" data={s.metacognition||{}}/><DataPanel title="Prediction" data={s.prediction||{}}/><DataPanel title="Safety" data={s.safety||{}}/></section></>}
function Chat({chat,msg,setMsg,send,busy}:any){return <section className="chatPanel"><div className="chatHead"><div><h2>Chat Workspace</h2><p>Interact with the cognitive loop using the active local model.</p></div><span className="modelPill"><span className="dot"/> LOCAL</span></div><div className="messages">{chat.length===0&&<div className="empty">Start a conversation with ConsciousCore.</div>}{chat.map((x:any,i:number)=><div key={i} className={'bubble '+x.role}><small>{x.role==='user'?'YOU':'CORE'}</small>{x.text}</div>)}</div><div className="composer"><input value={msg} onChange={e=>setMsg(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Message ConsciousCore..."/><button onClick={send} disabled={busy}>{busy?<RefreshCw className="spin" size={18}/>:<Send size={18}/>}</button></div></section>}
function DataPanel({title,data}:any){return <section className="panel dataPanel"><div className="panelTitle"><h2>{title}</h2><span>LIVE</span></div><pre>{JSON.stringify(data,null,2)}</pre></section>}
createRoot(document.getElementById('root')!).render(<App/>);
